name: PythonWorkflow

on:
  push:

jobs:
  test:
    runs-on: self-hosted
    steps: 
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        run: |
          cd backend
          python3 -m venv venv
          pip install -r requirements.txt
      - name: Tests
        run: pytest tests.py
          
    
    
